import React from "react"
import { css } from "@emotion/core"
import styled from "@emotion/styled"

const Container = styled.div`
  height: 70vh;
  display: flex;
  background-color: #513c00;
`

export default () => <Container>CONTACT US</Container>
